# Camera Configuration for Poultry Vision System
# Supports USB cameras and RTSP streams

cameras:
  # Top-down camera (primary view)
  top:
    name: top
    type: rtsp          # usb, rtsp, or file
    source: "rtsp://admin:Poultry2026!@10.80.187.200:554/h264Preview_01_main"
    width: 2560
    height: 1920
    fps: 30
    rotation: 0         # 0, 90, 180, or 270 degrees
    buffer_size: 30

  # Side-angle camera (secondary view)
  side:
    name: side
    type: usb
    source: "0"         # Logitech C920 on the Pi
    width: 960
    height: 720
    fps: 30
    rotation: 180       # Camera mounted upside down
    buffer_size: 30
    backend: v4l2
    fourcc: MJPG

# Frame synchronization settings
sync:
  tolerance_ms: 75      # Maximum timestamp difference for frame matching
  buffer_size: 30       # Ring buffer size for each camera
  primary_camera: side  # Lower-latency USB camera drives synchronization

# RTSP-specific settings
rtsp:
  transport: tcp        # tcp or udp
  reconnect_delay_s: 2  # Initial reconnect delay
  max_reconnect_delay_s: 30
