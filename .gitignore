# ── Python ─────────────────────────────────────────────────────────
*__pycache__/
*.pyc
*.pyo
.DS_Store

# ── Environments / editors / tools ─────────────────────────────────
.venv/
.vscode/
.claude
ultralytics.egg-info
# machine-local paths, never commit
ultralytics/settings.json

# ── Model weights ──────────────────────────────────────────────────
# Ignore all PyTorch weights. Hailo models under models/hailo/ (*.hef,
# labels.txt, README.txt) are NOT weights-by-extension and remain tracked
# so a Pi clone has the detection models ready.
*.pt

# ── Data / generated artifacts (regenerated locally; never committed) ─
dataset/
runs/
output/
hen_crops/
pipeline_figures/

# ── Hailo package tarballs (commit the extracted models/hailo/, not the tar) ─
poultry-pi-hef-package-*.tar.gz
